import React from 'react';
import './TaskForm.css';

const TaskForm = () => {
    return (
        <form className="task-form">
            {/* Task form content */}
        </form>
    );
}

export default TaskForm;
